---
interface Props {
  heading: string
  text?: string
}

const { heading, text, ...props } = Astro.props
---

<div class="mb-4" {...props}>
  <h2 class="font-sans font-medium text-xs text-faded tracking-[0.02em] uppercase">{heading}</h2>
  {text && <p class="mt-3 text-md">{text}</p>}
</div>
